<resources>
    <style name="push_style">
        <item name="android:hint">@string/style_hint</item>
        <item name="android:inputType">number</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="push_alias">
        <item name="android:hint">@string/alias_hint</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="push_mobilenumber">
        <item name="android:hint">@string/mobilenumber_hint</item>
        <item name="android:textSize">14sp</item>
    </style>
    <style name="push_tag">
        <item name="android:hint">@string/tag_hint</item>
        <item name="android:textSize">14sp</item>
    </style>


    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->

    <style name="AppTheme" parent="android:Theme.Light">


        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/qqtoppanelblue</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!--
    Base application theme, dependent on API level. This theme is replaced
    by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
-->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
        Theme customizations available in newer API levels can go in
        res/values-vXX/styles.xml, while customizations related to
        backward-compatibility can go here.
    -->
    </style>

    <style name="NoTitleDialog" parent="android:style/Theme.Dialog">

        <!--     <item name="android:windowFrame">@null</item>移除边框 -->
        <item name="android:windowNoTitle">true</item>去除顶部标题栏
        <item name="android:windowIsTranslucent">true</item>窗体透明
        <!--  <item name="android:background">@android:color/white</item>背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>窗体背景透明
        <item name="android:windowIsFloating">true</item>窗体是否浮动
        <item name="android:backgroundDimEnabled">true</item>背景是否昏暗
        <item name="android:backgroundDimAmount">0.3</item>昏暗数量
    </style>


    <style name="BlackBackDialog" parent="android:style/Theme.Dialog">
        <!--     <item name="android:windowFrame">@null</item>移除边框 -->
        <item name="android:windowNoTitle">true</item>去除顶部标题栏
        <item name="android:windowIsTranslucent">true</item>窗体透明
        <!--  <item name="android:background">@android:color/white</item>背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>窗体背景透明
        <item name="android:windowIsFloating">true</item>窗体是否浮动
        <item name="android:backgroundDimEnabled">true</item>背景是否昏暗
        <item name="android:backgroundDimAmount">1.0</item>昏暗数量
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
